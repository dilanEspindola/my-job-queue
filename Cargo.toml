[package]
name = "job-queue-rust"
version = "0.1.0"
edition = "2021"
default-run = "job-queue-rust"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }

[dependencies.uuid]
version = "1.23.0"
# Lets you generate random UUIDs
features = [
    "v4",
]